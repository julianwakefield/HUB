<h1>Add a Game</h1>
</div>
  <%= simple_form_for([@game]) do |f| %>
    <%= f.input :name%>
    <%= f.submit :submit  %>
    <%= f.input :photo, as: :file %>

  <% end %>
  <%# <%= link_to "Back", game_path %> %>
</div>